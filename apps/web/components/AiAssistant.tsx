"use client";
import { useState } from "react";
import { usePathname } from "next/navigation";
import { ChatCircleDots, PaperPlaneTilt, X } from "@phosphor-icons/react";
export function AiAssistant() { const pathname=usePathname(); const [open,setOpen]=useState(false); const [query,setQuery]=useState(""); const [answer,setAnswer]=useState(""); if(pathname==="/") return null; function ask(){ if(!query.trim()) return; setAnswer(`已为你整理「${query.trim()}」相关的商品与店铺。你可以从价格、库存和同步时间三个维度进行比较。`); } return <div className="assistant-wrap">{open && <section className="assistant-panel" aria-label="AI 助手"><div className="assistant-head"><div><strong>AI 比价助手</strong><small>只使用站内公开数据</small></div><button className="icon-button" aria-label="关闭 AI 助手" onClick={()=>setOpen(false)}><X /></button></div><div className="assistant-body">{answer || <p>告诉我你想找什么，我会帮你整理筛选条件。</p>}</div><div className="assistant-input"><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==="Enter"&&ask()} placeholder="例如：找最便宜的 Claude Pro" aria-label="向 AI 助手提问"/><button className="icon-button primary" aria-label="发送" onClick={ask}><PaperPlaneTilt weight="fill"/></button></div></section>}<button className="assistant-button" onClick={()=>setOpen(!open)} aria-label="打开 AI 比价助手"><ChatCircleDots weight="fill"/><span>AI</span></button></div>; }
